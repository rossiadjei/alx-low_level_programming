#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char apht;

	for (apht = 'a' ; apht <= 'z' ; ++apht)
	{
		putchar(apht);
	}
	putchar('\n');
	return (0);
}
